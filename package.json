{
  "name": "master-ip-session",
  "version": "1.0.0",
  "description": "MASTER-IP session generator and pairing portal",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js && node --check api/generate.js && node --check api/session.js && node --check api/status.js && node --check lib/auth.js && node --check lib/encrypt.js && node --check lib/pairing.js && node --check lib/session.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "baileys": "7.0.0-rc13",
    "hookified": "^2.1.1",
    "pino": "^9.6.0"
  }
}
